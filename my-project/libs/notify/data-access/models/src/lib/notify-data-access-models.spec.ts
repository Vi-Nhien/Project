import { notifyDataAccessModels } from './notify-data-access-models';

describe('notifyDataAccessModels', () => {
  it('should work', () => {
    expect(notifyDataAccessModels()).toEqual('notify-data-access-models');
  });
});
